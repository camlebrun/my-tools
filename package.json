{
  "name": "my_tools",
  "version": "1.0.0",
  "description": "Outils personnels optimisés pour la production",
  "main": "script.js",
  "scripts": {
    "build": "npm run build:css && npm run build:js && npm run build:html",
    "build:css": "cleancss -o styles.min.css styles.css && mv styles.min.css styles.css",
    "build:js": "uglifyjs script.js -o script.min.js --compress --mangle && mv script.min.js script.js",
    "build:html": "html-minifier-terser --collapse-whitespace --remove-comments --remove-optional-tags --remove-redundant-attributes --remove-script-type-attributes --remove-tag-whitespace --use-short-doctype --minify-css true --minify-js true *.html -o temp.html && for file in *.html; do mv temp.html $file; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/camlebrun/my-tools.git"
  },
  "keywords": ["tools", "web", "optimization"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/camlebrun/my-tools/issues"
  },
  "homepage": "https://github.com/camlebrun/my-tools#readme",
  "devDependencies": {
    "clean-css-cli": "^5.6.2",
    "uglify-js": "^3.17.4",
    "html-minifier-terser": "^7.2.0"
  }
}
